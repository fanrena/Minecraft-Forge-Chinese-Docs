资源包
==============

[资源包][respack] 通过 `assets` 目录自定义客户端资源，包括纹理、模型、声音、本地化等。你的模组（以及 Forge 本身）也可以拥有资源包。

资源包存储在项目的 `resources` 目录中。`assets` 目录包含包的内容，包本身由 `pack.mcmeta` 定义。模组可以有多个资源域。

详见 [Minecraft Wiki 资源包教程][createrespack]。

[respack]: https://minecraft.wiki/w/Resource_Pack
[createrespack]: https://minecraft.wiki/w/Tutorials/Creating_a_resource_pack
[resourcelocation]: ../../concepts/resources.md
